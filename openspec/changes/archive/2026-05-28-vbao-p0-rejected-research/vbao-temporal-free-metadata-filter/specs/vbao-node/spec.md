## REJECTED / HISTORICAL

This change was a candidate-only metadata-filter exploration. It is not an
active product requirement and SHALL NOT be read as permission to expose a
metadata-aware VBAO filter in the demo, benchmark API, `VBAONodeOptions`, or
`@horizonao/core` exports.

Current product direction is the closed `VBAONode` surface with raw debug output
via `getRawTextureNode()` and final product output via `getTextureNode()`.
