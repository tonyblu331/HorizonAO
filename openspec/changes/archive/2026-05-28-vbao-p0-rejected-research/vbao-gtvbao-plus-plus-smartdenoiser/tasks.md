# Tasks: GTVBAO++ SmartFilter

- [x] Add RED contracts for a demo-only `gtvbao++` filter and reference SmartFilter.
- [x] Extend the internal reference spatial-filter helper with bitmask-aware rejection.
- [x] Add the WebGPU SmartFilter pass in `MuseumScene.tsx`.
- [x] Wire benchmark and E2E internals.
- [x] Capture WebGPU screenshots/JSON for raw, generic, metadata-aware, and `gtvbao++`.
- [x] Update evidence.
- [x] No production build.

## Next tasks

- [x] Add per-tap bitmask metadata texture/channel after `gtvbao++` remained too close to raw VBAO.
- [x] Capture per-tap metadata WebGPU evidence and compare against the center-only `gtvbao++` run.
- [x] Compare against an internal SSILVB/reference formula ablation before any formula change.
- [x] Record per-tap filter decision gate artifact.
