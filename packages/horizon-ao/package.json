{
  "name": "@horizonao/core",
  "version": "0.0.0",
  "description": "Core settings and future WebGPU pass primitives for HorizonAO.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsdown",
    "test": "vitest run --root ../.. packages/horizon-ao/src packages/horizon-ao/reference",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.reference.json",
    "typecheck:tsgo": "tsgo --noEmit && tsgo --noEmit -p tsconfig.reference.json"
  },
  "peerDependencies": {
    "three": ">=0.184.0"
  },
  "devDependencies": {
    "@types/three": "0.184.1",
    "tsdown": "0.22.0"
  }
}
